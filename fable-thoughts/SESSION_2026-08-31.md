# Session 2026-08-31 — external audit: the last unmeasured cells closed, watchdog baselined

Independent pass over the whole branch (Capy). Method: re-read every argument, then run the
items the branch itself lists as unfinished, on fresh data and fresh executed buys. Demo
account DOT93131975, public feed via `deriv_api.py`, all fetches through the integrity-checked
`derivfetch.fetch_ticks`.

Verdict up front: **the core arguments all survive audit.** The information bound, the
linearity/convexity closures, the entry-boundary argument and the proposal-lie methodology are
correct and correctly applied. What follows closes the open cells and corrects one framing.

## 1. The last three unmeasured digit symbols are uniform (closed)

1HZ15V, 1HZ30V, 1HZ90V — flagged in ADDENDUM 2026-08-29b as the last unmeasured cells.
200k fresh ticks each:

| symbol | sigma_pips | digit chi2 (9 df) | MI (MM-corr) | shuffled |
|---|---:|---:|---:|---:|
| 1HZ15V | 353.6 | 20.2 | 0.000019 | 0.000040 |
| 1HZ30V | 349.6 | 13.0 | 0.000002 | −0.000002 |
| 1HZ90V | 3031.3 | 7.5 | 0.000009 | −0.000005 |

Sigma is 2–3 orders of magnitude above the concentration threshold; MI is indistinguishable
from its own shuffle. As expected. **All 20 digit-offering symbols are now measured.**

## 2. Watchdog run with executed buys: no reverse lie anywhere (baselined)

`watchdog.py --execute --stake 10` across all 20 digit symbols (one real $10 demo buy each):

- **Executed = proposal on 16 symbols at 1.9530** (uncut grid intact on JD10/25/50/75,
  R_10/25/50/75, 1HZ15/25/30/50/75/90V, RDBULL, RDBEAR).
- The three pre-emptive cuts hold: 1HZ100V, 1HZ10V, R_100 execute **1.9230** (proposal still
  quotes 1.9530 — the 1.6% overstatement is stable).
- JD100 executes **1.7940** (proposal 1.9530, 8.9% overstatement). Sigma 3.79 — the only
  low-sigma alert in the book.
- **`executed > proposal` occurs nowhere.** The one condition that would be free money is
  clean today. `results/watchdog_registry.json` is now committed as the diff baseline —
  the previous runs never persisted one, so condition-diffing starts from here.

## 3. Six symbols the branch never catalogued (new-launch category, closed)

`active_symbols` carries six symbols absent from every document in this branch:
**RB100, RB200** (Range Break) and **BOOM150N, BOOM300N, CRASH150N, CRASH300N**. All tick
at 1/s. Contract surface:

| symbols | offered |
|---|---|
| RB100, RB200 | MULTUP/MULTDOWN only |
| BOOM/CRASH 150N/300N | ACCU + MULTUP/MULTDOWN only |

No digits, no binaries. **The information/availability anticorrelation law holds on the
launches too** — these are exactly the engineered-asymmetry instruments, and the digit and
directional surfaces are withheld. Closed by availability; nothing to price. They are in the
watchdog registry now, so a contract-surface change on any of them will alert.

## 4. The OVER5 loose end, closed empirically per sigma band

FINDINGS §10 scored JD100 OVER5/UNDER4 (executed 2.186) at MI/need = 0.47 using the
**average** MI, while ADDENDUM 2026-08-29 showed the best sigma quintile carries 3x the
average MI. Nobody combined the two: at best-band MI 0.0076 bits vs 0.0052 needed, the cell
was nominally open. Measured directly instead, on 600k fresh ticks (6.9 days, spot 210–258,
affine sigma 3.72–4.55), executed payouts, Wilson-99:

| band | n | ±2-window @1.794 (BE 55.74%) | 4-window @2.186 (BE 45.75%) |
|---|---:|---:|---:|
| σ 3.60–3.79 | 23,559 | p=0.5242, EV −5.96% | p=0.4218, EV −7.80% |
| σ 3.79–3.90 | 125,902 | p=0.5206, EV −6.61% | p=0.4187, EV −8.48% |
| σ 3.90–4.10 | 244,513 | p=0.5161, EV −7.42% | p=0.4154, EV −9.20% |
| σ ≥ 4.10 | 206,025 | p=0.5100, EV −8.51% | p=0.4086, EV −10.67% |

Every band, every Wilson-99 upper edge, negative. Best single cells are the centered-window
digits as the wrapped normal predicts (d=7: −0.22%, d=2: −1.40% at 1.794 in the lowest band)
— still under water. **The MI-side loophole is closed by direct measurement: the 2.186 rung
is priced.**

(Replication note: the d∈{2,7} outperformance shows up at z ≈ +10 on this sample and in both
halves. That is not new structure — those are the two digits where the fixed {0–4}/{5–9}
win-sets are exactly centered on the entry digit, i.e. pure wrapped-normal geometry, as the
Aug-13 session already stated. Recorded here so nobody rediscovers it as an anomaly.)

## 5. The reopening threshold, computed instead of implied

Nobody ever wrote down the spot level at which the CUT grid goes positive again. Wrapped
normal: the ±2 window mass reaches the 1.794 breakeven (55.74%) at **sigma* = 3.50**, i.e.
spot ≈ 198 by the 46-day affine law. Empirically the threshold is lower: the Aug-13 fresh
sample at spot ~194 (σ 3.2–3.5) measured pooled p = 0.5408, because realized P runs
0.3–1.4pp under the model — so the honest reopening trigger is nearer **spot ≈ 185–190**,
best cells first (d=2/d=7 clear earliest; Aug-13 measured d=2 at 0.5654 > BE already).

Timing physics from spot 217.5 (today), drag −0.186%/day, daily vol 6.09%:

- median time to spot 198 ≈ 46 days; to 188 ≈ 73 days; P(hit within 90d) ≈ 0.5–0.6.
- Deriv's demonstrated repricing latency: four cuts in eight days, plus pre-emptive cuts on
  the three next-lowest-sigma symbols at σ ≈ 14–17, where digits are still uniform.

That asymmetry is the structural statement this branch circles but never writes down:
**edge lifetime = min(τ_drift, τ_reaction), with τ_drift measured in months and τ_reaction
measured in days.** A counterparty that owns the generator, the payout grid, and the
availability matrix, and has demonstrably learned to reprice ahead of the physics, wins the
war of attrition by construction. The JD100 window existed only because their reaction time
was once ~46 days; it is now ~days. Waiting for σ* is therefore not a strategy — it is a
lottery ticket whose prize the counterparty can cancel after you win it.

## 6. Posture

Unchanged from PROJECT_BRIEF §9, with one correction: a monitoring tool that nobody schedules
is a document, not a watchdog. The registry is now committed; the remaining work is a cron
(6–12h cadence, read-only mode is sufficient — grid changes show in proposals for the uncut
symbols, and the three known liars have stable offsets). Alert conditions stay exactly as
built: new symbol / digit availability change, σ < 5 on a digit symbol, pip_size change,
executed>proposal, plus (new, from §5) **JD100 spot < 200** as the advance warning that the
best-cell reopening race is on.

Nothing else on this book is open. The last unmeasured cells (§1), the last untested pricing
direction (§2), the uncatalogued launches (§3) and the last MI loophole (§4) all closed
negative in one session, which is exactly what the closing arguments predicted they would do.
